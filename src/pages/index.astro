---
import App from "#app";
import Layout from "#layout";

const title = "Guess the Package";
const description = "Guess the npm package from its dependency tree";
---

<Layout {title} {description}> <App client:load /> </Layout>
